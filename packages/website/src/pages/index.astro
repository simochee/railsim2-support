---
import HelpLayout from "../layouts/HelpLayout.astro";
import { getHelpPages } from "../lib/vendor";

const index = getHelpPages().find((p) => p.slug === "index")!;
---

<HelpLayout title="RailSim II Manual" description={index.description} isTop>
  <Fragment set:html={index.body} />
</HelpLayout>
